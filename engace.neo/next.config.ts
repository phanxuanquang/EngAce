// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placehold.co', 'i.imgur.com'], // Thêm domain ảnh này vào đây
  },
};

module.exports = nextConfig;
